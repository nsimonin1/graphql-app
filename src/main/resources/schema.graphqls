type Query {
  bookById(code: ID): Book 
  authorById(code: ID): Author
}

type Book {
  code: ID
  name: String
  pageCount: Int
  author: Author
}

type Author {
  code: ID
  firstName: String
  lastName: String
}